apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: ise-connector-db
spec:
  instances: 3
  monitoring:
    enablePodMonitor: true
  storage:
    size: 1Gi
  bootstrap:
    initdb:
      database: ise-connector_db
      owner: ise-connector
